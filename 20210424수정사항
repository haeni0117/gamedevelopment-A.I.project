원래는 코루틴 함수가 진행중일 때에 해당하는 정수값이든 문자열이든 특정값을 받아서 더 이상 그 값이 아닐 때 버튼이 생성되게 하려고 했는데,
정수형은 0으로 문자열은 null로 초기화된 상태 그대로가 console창에 동시에 출력되는 것을 발견했다.
값의 변화를 통해 상태변화를 확인하려고 했는데 코루틴 함수가 동작중일 때 정수형이 4였다면 콘솔창에 04040404... 이런 식으로 출력이 되었다. 코루틴 함수의 값과 코루틴 함수 외부의 값이 동시에 출력되고 있었던 것이다.
그 값을 콘솔창에 출력한 방법은 update함수에 Debug.log(변수명);을 통해 콘솔창에 찍었다. 
사용자가 화면을 몇 번 터치할 수 있는지는 내가 제어할 수 있는 문제가 아니라 사용자의 마음이기 때문에 이상적인 상황이 아닐 때에도 정상적으로 작동하게 해야한다. 
sol1) 버튼의 투명도를 최고로 하여 코루틴 함수가 진행되는 동안으냐 그 상태가 지속되어야함(button의 interactable사용)
